{
  "name": "narciso",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "chat": "node --env-file-if-exists=.env src/cli.mjs",
    "start": "node --env-file-if-exists=.env src/photon.mjs",
    "doctor": "node --env-file-if-exists=.env scripts/doctor.mjs",
    "google:login": "node --env-file-if-exists=.env scripts/google-login.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "googleapis": "181.0.0",
    "spectrum-ts": "12.8.0",
    "zod": "^3.25.0"
  },
  "overrides": {
    "@opentelemetry/core": "2.10.0"
  }
}
